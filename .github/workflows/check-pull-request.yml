name: Check Pull Request

on:
  pull_request:
    branches:
      - main
      - develop
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - ready_for_review

jobs:
  pr-validator:
    name: Run Pull Request Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: |
          npm ci
          npm run build:frontend
          npm run format:check
          npm run lint

      - name: Security audit
        run: npm run security-audit

      - name: Test Docker Image Build
        run: |
          set +e
          docker build --no-cache --tag cas-mis .
          exit $?

      - name: Run Tests
        run: |
          mkdir -p coverage
          docker compose -f compose.yaml -f compose.test.yaml run --name cads-mis-test-run "cads-mis"

      - name: Extract coverage report
        if: always()
        run: |
          docker cp cads-mis-test-run:/home/node/coverage/. ./coverage/ 2>/dev/null || true
          docker rm cads-mis-test-run 2>/dev/null || true
          if [ -f coverage/lcov.info ]; then
            sed -i 's|SF:/home/node/|SF:|g' coverage/lcov.info
          fi

      - name: SonarCloud Scan
        if: github.actor != 'dependabot[bot]'
        uses: SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true
